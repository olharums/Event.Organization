import styled from "styled-components";

export const Text = styled.p`
  font-size: x-large;
  text-align: center;
`;

export const ErrorMessage = styled.p`
  font-size: medium;
  color: red;
  margin: 0 0 1rem 0;
`;
